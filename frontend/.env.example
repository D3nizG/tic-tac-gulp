# Copy to .env.local for local development.

# Backend Socket.IO / API URL
# Local dev (leave empty — Vite proxy handles it):
VITE_SOCKET_URL=

# Production (set this when deploying the frontend):
# VITE_SOCKET_URL=https://api.tic-tac-gulp.d3nizg.dev
